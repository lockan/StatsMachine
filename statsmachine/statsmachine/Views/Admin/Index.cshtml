@{
    ViewBag.Title = "Admin";
}

<h2>@ViewBag.Title</h2>

<div class="nav">
    <ul class="nav nav-pills nav-stacked">
        <li> @Html.ActionLink("Users", "Index", "Users", new { area = "" }, new { }) </li>
        <li> @Html.ActionLink("Game Systems", "Index", "GameSystems", new { area = "" }, new { }) </li>
        <li> @Html.ActionLink("User Games", "Index", "UserGames", new { area = "" }, new { }) </li>
    </ul>
</div>
